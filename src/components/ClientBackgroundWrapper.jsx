"use client";
import BackgroundWrapper from "@/components/BackgroundWrapper";

export default function ClientBackgroundWrapper({ children }) {
  return <BackgroundWrapper>{children}</BackgroundWrapper>;
}
